#include <iostream>
using namespace std;

int main(){
    cin.tie(NULL);
    cout.tie(NULL);
    cin.sync_with_stdio(false);

    long long a, b, c;
    cin >> a >> b >> c;

    cout << a + b + c << "\n";
}