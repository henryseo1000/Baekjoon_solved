#include <iostream>
#include <string.h>
using namespace std;

long sum(std::vector<int> &a){
    long sum = 0;
    for (int i = 0; i < a.size(); i++){
        
    }
    return sum;
}

int main(){
    cin.tie(NULL);
    cout.tie(NULL);
    cin.sync_with_stdio(false);
    int a;
    cin >> a;


}